关于一些二维数组的知识
1.针对某个元素（row,col），可以用dx = { -1, 0, 0, 1 };dy = { 0, -1, 1, 0 };
并用一个循环来访问其上下左右的邻居。注意dx[i],dy[i]是对应的。

2.对于原始大方格，建立时就多加两个小格，其作用是：将两个多出的格子作为虚拟格子，与其俩根节点相同的格子
一定是percolation的

3.对于这种有着对应状态的（true or false），可以再另创建一个二维数组与之对应，储存对应格子的状态。

4.二维矩阵的行列数转为索引值是有公式的，索引从零开始还是一开始公式有所不同。

5.暂时就这吧。
